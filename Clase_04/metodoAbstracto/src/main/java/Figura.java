public abstract class Figura {
    // Atributos comunes a todas las figuras, si los hubiera

    // Constructor, si es necesario

    // Método abstracto
    public abstract double calcularArea();

    // Otros métodos no abstractos, si los hubiera
}